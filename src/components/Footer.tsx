import Cross from "./icons/cross"
import Tick from "./icons/tick"

const Footer = () => {
  return (
   <section className='w-screen h-[50vh] flex flex-col md:flex-row'>
    
   </section>
  )
}

export default Footer
